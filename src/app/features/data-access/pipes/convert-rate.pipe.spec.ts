import { ConvertRatePipe } from './convert-rate.pipe';

describe('ConvertRatePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertRatePipe();
    expect(pipe).toBeTruthy();
  });
});
