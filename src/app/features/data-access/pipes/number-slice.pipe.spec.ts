import { NumberSlicePipe } from './number-slice.pipe';

describe('NumberSlicePipe', () => {
  it('create an instance', () => {
    const pipe = new NumberSlicePipe();
    expect(pipe).toBeTruthy();
  });
});
